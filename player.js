class Player {
  constructor(id, token, turn) {
    this.id = id;
    this.token = token;
    this.wins = [];
    this.turn = turn;
  };

  saveWinsToStorage() {

  };

  retrieveWinsFromStorage() {

  };
};